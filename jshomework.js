// Challenge 1 //

let cups_consumed = 4;
if (cups_consumed < 3) {
    console.log ("Yes, I'll take another cup of coffee");
    }
    else {console.log ("I think I'm okay for now.");
}

// Challenge 2 //

let miles = 15;
    if (miles > 500) {
        console.log ("Yes, I think I need an oil change.");
    }
    else { console.log (`My car doesn't need an oil change yet. It will in ` + (500 - `${miles}`) + ` miles.`);
    }